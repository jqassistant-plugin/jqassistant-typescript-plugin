import { AliasAliasClass, IndexClass, IndexEnum, IndexFunction, IndexInterface, IndexType, IndexVar } from "./subdir";

let a: IndexClass;
let b: IndexInterface;
let c: IndexType;
let d = 4 + IndexVar;
let e: IndexEnum;
let f: AliasAliasClass;
let g = IndexFunction();
